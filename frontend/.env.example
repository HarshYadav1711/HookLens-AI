# HookLens AI — frontend environment
# Copy to .env.local and adjust for your environment.

# Backend API origin (no trailing slash).
# Local default matches uvicorn on port 8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
